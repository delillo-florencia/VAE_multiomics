from setuptools import setup, find_packages

setup(
    name="VAE_multiomics",
    version="0.1.0",
    description="Tools for preprocessing and training multimodal autoencoders on gene and isoform expression data")
